server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pago_db_dev?createDatabaseIfNotExist=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root  # <-- Pon aquí la contraseña de tu MySQL local
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect