server:
  port: 8092

spring:
  application:
    name: cliente-service

  datasource:
    url: jdbc:mysql://mysql:3306/clientedb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    enabled: false
